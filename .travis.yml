git:
  depth: 1

language: node_js
node_js:
  - "10"

install: npm install

script: npm run build

after_script:
  - "cat ./coverage/lcov.info | ./node_modules/.bin/coveralls"

notifications:
  email:
    recipients:
      - aleksander.popov.93@gmail.com